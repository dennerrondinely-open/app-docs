# Geru App Docs

Site de documentação em `Fumadocs` para o projeto `geru-app`.

## Rodando localmente

```bash
cd docs
npm install
npm run dev
```

Abra `http://localhost:3000`.

## Build de produção

```bash
cd docs
npm run build
npm run start
```
